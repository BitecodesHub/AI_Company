// (admin) layout — workspace/org admin and super-admin.
// Populated in Phase 9 (P9-01, P9-02).
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
