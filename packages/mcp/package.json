{
  "name": "@bitecodes/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP client manager, server host, tool approval and signing helpers",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "@bitecodes/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "1.0.4",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@bitecodes/config": "workspace:*",
    "typescript": "5.6.3",
    "vitest": "2.1.8"
  }
}
